p You need to be logged in, bastard!
if error
  p Error : #{error}

form(method="post", action="/login")
  input(type="text", name="login", placeholder="Login", autofocus, required, value="#{login}")
  //input(type="text", name="login", placeholder="Login", required, value="#{login}")
  input(type="password", name="password", placeholder="Password", required, value="")
  input(type="submit")

form(method="post", action="/redmine-key")
  input(type="text", name="key", placeholder="Redmine API key", required, value="")
  //input(type="text", name="key", placeholder="Redmine API key", autofocus, required, value="")
  input(type="submit")
